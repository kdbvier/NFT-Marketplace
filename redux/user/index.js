import {
  setUserInfo,
  setSideBar,
  setUserLoading,
  handleNewUser,
} from './action';
import { userSlice } from './slice';

export { userSlice, setUserInfo, setSideBar, setUserLoading, handleNewUser };
