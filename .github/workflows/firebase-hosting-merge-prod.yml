# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Manually deploy to Firebase Hosting on merge

on: [workflow_dispatch]

env:
  REACT_APP_ENV: ${{secrets.REACT_APP_ENV_PROD}}
  REACT_APP_API_ENDPOINT: ${{secrets.REACT_APP_API_ENDPOINT_PROD}}
  REACT_APP_WEB_SOKET: ${{secrets.REACT_APP_WEB_SOKET_PROD}}
  REACT_APP_FILE_SERVER_ENDPOINT: ${{secrets.REACT_APP_FILE_SERVER_ENDPOINT_PROD}}
  REACT_APP_WEBHOOK_URL_ETHEREUM: ${{secrets.REACT_APP_WEBHOOK_URL_ETHEREUM_PROD}}
  REACT_APP_WEBHOOK_URL_POLYGON: ${{secrets.REACT_APP_WEBHOOK_URL_POLYGON_PROD}}
  REACT_APP_ETHEREUM_QUICKNODE_URL: ${{secrets.REACT_APP_ETHEREUM_QUICKNODE_URL_PROD}}
  REACT_APP_POLYGON_QUICKNODE_URL: ${{secrets.REACT_APP_POLYGON_QUICKNODE_URL_PROD}}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build:prod
        env:
          CI: false
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_APOLLO_CREABO_PROD }}"
          channelId: live
          projectId: apollo-creabo-prod
