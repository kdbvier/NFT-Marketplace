# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - develop

env:
  REACT_APP_ENV: ${{secrets.REACT_APP_ENV_DEV}}
  REACT_APP_API_ENDPOINT: ${{secrets.REACT_APP_API_ENDPOINT_DEV}}
  REACT_APP_WEB_SOKET: ${{secrets.REACT_APP_WEB_SOKET_DEV}}
  REACT_APP_FILE_SERVER_ENDPOINT: ${{secrets.REACT_APP_FILE_SERVER_ENDPOINT_DEV}}
  REACT_APP_WEBHOOK_URL_GOERLI: ${{secrets.REACT_APP_WEBHOOK_URL_GOERLI_DEV}}
  REACT_APP_WEBHOOK_URL_MUMBAI: ${{secrets.REACT_APP_WEBHOOK_URL_MUMBAI_DEV}}
  REACT_APP_GOERLI_QUICKNODE_URL: ${{secrets.REACT_APP_GOERLI_QUICKNODE_URL_DEV}}
  REACT_APP_MUMBAI_QUICKNODE_URL: ${{secrets.REACT_APP_MUMBAI_QUICKNODE_URL_DEV}}
  REACT_APP_PINATA: ${{secrets.REACT_APP_PINATA}}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build:dev
        env:
          CI: false
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_APOLLO_CREABO_DEV }}"
          channelId: live
          projectId: apollo-creabo-dev
